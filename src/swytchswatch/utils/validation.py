from pathlib import Path
from typing import Union

# from .color_format import ColorCode


def has_required_keys(toml_file: Union[str, Path]) -> bool:
    is_valid = True  # TODO: implement

    return is_valid
